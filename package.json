{
  "name": "data-changes-council",
  "version": "0.1.0",
  "description": "Evidence-aware multi-model deliberation with visible dissent, policy gates, and human approval.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "dev:server": "tsx src/dev-server.ts",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:llms": "tsx scripts/test-all-llms.ts"
  },
  "keywords": [
    "llm",
    "multi-model",
    "deliberation",
    "ai-governance",
    "human-in-the-loop",
    "decision-support"
  ],
  "license": "Apache-2.0",
  "dependencies": {
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "dotenv": "^16.4.5",
    "tsx": "^4.19.2",
    "typescript": "^6.0.3",
    "vitest": "^3.1.4"
  }
}
